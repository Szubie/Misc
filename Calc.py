#To create a calculator that can deal with unlimited inputs? Use the same trick as before. Store user input in a string, split it apart at the operator, parse to ints. Calculate.
#Not that hard. But when dealing with Java's unfamiliar syntax, awkward.

raw_input("Please enter a mathametical operation: ")
